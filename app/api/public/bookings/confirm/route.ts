import { NextResponse } from "next/server";

// POST /api/public/bookings/confirm
// Phase 3: Booking confirmation — converts a hold into a confirmed booking.
// Stub until Phase 3 is built.

export async function POST() {
  return NextResponse.json(
    { error: "Booking confirmation not yet implemented." },
    { status: 501 },
  );
}
